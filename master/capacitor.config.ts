import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Gabriel Jackson MSD 4.2A',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
